{
  "name": "oni-vision-plugin",
  "version": "0.1.0",
  "description": "MCP server + skill that lets Claude query an oni-vision SQLite database.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --no-warnings=ExperimentalWarning mcp/server.js",
    "test": "node --no-warnings=ExperimentalWarning --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
